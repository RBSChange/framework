In this folder, for each task declared, you can find files named:
- <taskName>.start, a file which mtime and contents is the date when the task started
- <taskName>.pid, a file containing the pid of the (running) task
- <taskName>.end, a file which mtime and contents is the date when the task ended