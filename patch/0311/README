Version: 3.0.2

Changement de la resolution des icons:
	1) WEBEDIT_HOME/changeicons
	2) WEBEDIT_HOME/media/changeicons
	3) Action media/GetIcon qui utilise la variable de config : modules/media/icons/library
	4) Status: 404 File Not Found
	
Ajout d'un nouveau remplacement '{IconsBase}' au lieu de '{HttpHost}/icons' utilisable dans les bindings xml

Dans les feuilles de style utiliser url(/changeicons/ au lieu de url(/icons/

Convertion de la structure du dossier:

Constantes obsoletes : WEBAPP_HOME, AG_WEBAPP_DIR

Nouvelles constantes : 
 	- PROJECT_OVERRIDE (surcharge des modules) WEBEDIT_HOME/override
 	- DOCUMENT_ROOT (emplacement de la racine du site) WEBEDIT_HOME ou WEBEDIT_HOME/webapp/www
 	- WEB_CACHE_DIR (emplacement des fichiers de cache accesible depuis le web) WEBEDIT_HOME/cache/www
 	
Methode obsolete:
    - f_util_FileUtils::buildWebappPath() return la meme chose que buildOverridePath()
    - f_util_FileUtils::buildWebeditPath('webapp', ...)

Nouvelles methodes:
    - f_util_FileUtils::buildOverridePath() -> PROJECT_OVERRIDE
    - f_util_FileUtils::buildDocumentRootPath() -> DOCUMENT_ROOT
    - f_util_FileUtils::buildWebCachePath() -> WEB_CACHE_DIR
  
